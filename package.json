{
  "name": "dbde-ai-frontend",
  "version": "8.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node -e \"require('node:fs').rmSync('static/dist/assets', { recursive: true, force: true })\" && vite build",
    "preview": "vite preview",
    "build:prod": "node -e \"require('node:fs').rmSync('static/dist/assets', { recursive: true, force: true })\" && vite build --mode production"
  },
  "dependencies": {
    "docx": "^8.6.0",
    "dompurify": "^3.3.2",
    "microsoft-cognitiveservices-speech-sdk": "^1.48.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^6.0.0"
  }
}
